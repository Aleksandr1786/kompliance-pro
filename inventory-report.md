# Инвентаризация: kompliance-pro (desktop) vs kompliance-web

Отчёт только-чтение, без правок кода. Дата: 2026-07-28.

## 1. Есть в десктопе, нет в вебе

| Что | Файл (desktop) | Переносить в веб? |
|---|---|---|
| Аудит актуальности НПА-цитат (проверка правильности/устаревания номеров актов, отдельно от ленты новых изменений) | `src/modules/npa-audit.js`, `main.js` (`checkNpaCitations`, `NPA_WATCHLIST`, обращения к pravo.gov.ru) | Да, имеет смысл — веб уже мониторит НПА (`backend/npa-monitor.js`), но не проверяет актуальность уже занесённых номеров. Полезный технический экран для команды. |
| Демо-режим с выбором роли и интерактивным туром | `src/modules/demo.js`, `src/modules/demo-data.js` | Специфично для offline-знакомства с приложением; в вебе можно сделать аналог (демо-аккаунт), но не 1:1 перенос кода — архитектура другая (SPA с бэкендом). |
| Онбординг первого запуска + "утренний дайджест" при старте приложения | `src/modules/onboarding.js` (`checkOnboarding`, `showMorningDigest`) | Частично уместно в вебе (онбординг для новых пользователей), но "утренний дайджест по расписанию" в вебе не имеет прямого аналога отдельного файла — стоит проверить, не размазан ли он по `App.jsx`. |
| Гибкий импорт сотрудников из файла с ручным маппингом колонок (для разных выгрузок 1С) на уровне отдельного модуля с сохранением маппинга per-клиент | `src/modules/employee-import.js` | В вебе похожая функциональность есть (см. п.3 — массовый импорт нескольких организаций по Excel/CSV в `App.jsx`), но нужно сверить, сохраняется ли маппинг колонок между сессиями так же, как на десктопе. Возможно частичное дублирование логики, а не полное отсутствие — стоит уточнить у человека. |
| Лицензирование/триал через локальный ключ активации (offline-механизм: `trial_status`, `license_key`, `license_machine`, привязка к машине) | `main.js` (~104-445, `checkTrial`), `src/modules/settings-page.js` (экран активации лицензии) | Специфично для десктопа/electron-updater — веб продаёт доступ через `subscription.js` (фазы trial/grace/readonly/active), это принципиально другая модель монетизации, переносить нечего. |
| Файловая блокировка БД (`lock-utils.js`) — защита от одновременного запуска нескольких копий приложения с одним файлом lowdb | `lock-utils.js` | Специфично для локальной файловой БД Electron; в вебе БД — PostgreSQL с обычной многопользовательской работой, аналог не нужен. |
| MCP-сервер для работы с БД клиента извне (Claude Desktop и т.п.), включая мост записи (`mcp-write-bridge.js`) | `mcp-server/server.js`, `mcp-write-bridge.js` | Специфично для десктопной модели данных (локальный lowdb-файл на диске пользователя); в вебе нет прямого смысла — там уже есть `pg-doc-runner.js` как мост к PostgreSQL для похожих задач. |
| Полностью локальный readiness-calc как отдельный клиентский JS-модуль (`src/modules/readiness-calc.js`), подключаемый напрямую в `index.html` | `src/modules/readiness-calc.js` | Не отсутствует по сути — веб имеет `backend/readiness-calc-server.js`, идентичный по формулам (см. раздел 3, синхронизация 17.06.2026). Упомянуто здесь для полноты, т.к. это не "новая" фича, а другая точка подключения того же кода. |

## 2. Есть в вебе, нет в десктопе

| Что | Файл (web) | Переносить в десктоп? |
|---|---|---|
| Публичные страницы оферты и реквизитов (`/offer`, `/contacts`) для требований ЮKassa | `frontend/src/PublicPages.jsx` | Не нужно — специфично для приёма платежей на сайте SaaS-версии, у десктопа нет встроенного эквайринга. |
| Тарифы/подписки/лимиты, фазы (trial/grace/readonly/active), расчёт доплаты при смене тарифа | `backend/subscription.js` | Не нужно переносить как есть — десктоп использует локальную лицензию (см. раздел 1). Модели монетизации разные по архитектуре. |
| Telegram-бот и MAX-бот для сотрудников (напоминания, прохождение тестов по СОУТ/ОТ через `/start` и т.п.) | `backend/bot.js`, `backend/max-bot.js` | Требует постоянно работающего сервера — не подходит для desktop-приложения на машине пользователя (бот должен быть доступен 24/7). Специфично для веба. |
| Мониторинг НПА с фильтрацией через DeepSeek (лента изменений законодательства, отдельная от аудита) | `backend/npa-monitor.js`, `backend/npa-watchlist.js` | В десктопе есть похожая, но встроенная в `main.js` логика (`checkNpaUpdates`, `NPA_WATCHLIST` там же) — то есть функция есть, но не вынесена в отдельный переиспользуемый модуль с внешним AI-фильтром. Не столько "нет", сколько "реализовано иначе" — см. раздел 3. |
| Центр обучения с генерацией протокола/черновика приказа об увольнении при неудачном тестировании (`training-docs.js`, ссылка на ст. 76 ТК РФ) | `backend/training-docs.js` | Десктоп имеет UI-панель "Протокол проверки знаний" в `client-card.js` (строки ~407-415), но нет подтверждения автогенерации приказа именно по неудовлетворительному результату с привязкой к `training_sessions` — похоже на частичный аналог. Стоит уточнить и при необходимости перенести логику генерации в десктоп, если там её действительно нет как автоматики (в отчёте фиксируется как вероятный пробел, не стопроцентно подтверждённый). |
| Раздел "разница цены при смене тарифа" / модель "3 из 4 направлений = цена пакета всё включено" | `backend/subscription.js` (`PLAN_PRICES`/`MODULE_PRICES`/`PACK_PRICES`) | Не нужно — коммерческая логика веб-подписки, у десктопа нет модульных апгрейдов такого рода. |
| Региональная отчётность жёстко ограничена Краснодарским краем (`REGIONAL_REPORTS`) | упомянуто в `backend/index.js` / CLAUDE.md веба | Нужно проверить, есть ли в десктопе вообще функциональность региональной отчётности — по беглому просмотру `src/modules/reporting.js` не встретилось явного аналога `REGIONAL_REPORTS`; если региональная отчётность нужна и в десктопе — это кандидат на перенос. |

## 3. Реализовано в обеих, но по-разному (источники рассинхрона)

| Функция | Desktop | Web | Комментарий |
|---|---|---|---|
| **Нормализация города в приказах (`norm()` в gen_p1.js)** | `gen_p1.js:257` (та же функция целиком идентична, кроме regex): `c.city = rawCity.replace(/^г\.?\s*/i,'').replace(/^город\s*/i,'').trim()` | `gen_p1.js:257`: `c.city = rawCity.replace(/^г(?:\.\s*|\s+)/i,'').replace(/^город\s+/i,'').trim()` | **Баг НЕ исправлен в десктопе.** Старое выражение `/^г\.?\s*/i` срезает букву "г" даже когда после неё сразу идёт другая буква (нет точки/пробела) — «Геленджик» → «еленджик», «Грозный» → «розный» и т.п. В вебе исправлено 28.07.2026 (см. `backend/CLAUDE.md`), в desktop `gen_p1.js` и `doc-generation.js` — старая версия. Веб-версия правильнее. |
| **Та же нормализация города в сборке пакета документов** | `doc-generation.js:97`: `.replace(/^г\.?\s*/i, '')` — тот же баг, плюс вообще не обрабатывает вариант "город Геленджик" (нет `.replace(/^город.../)`) | `doc-generation.js:102-109`: исправленный regex + отдельная обработка префикса "город " | Тот же баг что и выше, но с ещё одним отличием: десктопная версия вообще не убирает префикс "город " (только "г."), в отличие от `gen_p1.js`, где такая обработка есть — то есть в десктопе сама функция `norm()` в `gen_p1.js` и функция подготовки данных в `doc-generation.js` уже расходятся друг с другом внутри одной базы. |
| **Плейсхолдер интерфейса в тексте документа "Табель постов охраны" (ЧОП)** | `gen_chop.js:139` | `gen_chop.js:139` — **идентичный текст, идентичная строка** | Обнаружено, что это НЕ отличие между базами — баг присутствует одинаково в обеих: если у клиента нет заполненных ЧОП-данных сотрудников, в итоговый .docx документ (официальный "Табель постов охраны") подставляется текст `«Данные о постах не заполнены — заполните разряд/тип поста/режим смен в карточках сотрудников (иконка ЧОП).»` — это инструкция для оператора интерфейса, а не корректный юридический текст, и она попадёт в готовый документ, если пользователь сгенерирует пакет до заполнения карточек. Ровно тот же класс проблемы, что и раньше находили в вебе (не исправлено ни там, ни там). |
| **Формулы готовности (readiness): ОТ/ПДн/ВУ/ЧОП** | `mcp-server/readiness-calc.js` (103 строки) | `backend/readiness-calc-server.js` (92 строки) | **Синхронизировано.** Текстовое сравнение показывает идентичные формулы `calcOtReadiness`, `calcPdReadiness`, `calcVuReadiness`, `calcChopReadiness` — веб явно комментирует, что это "серверный порт" десктопной версии от 17.06.2026, логика не менялась. Разница только техническая (module.exports вместо глобальных function declarations). Единственная оговорка из комментария веба: `docsOt` на сервере приходит в формате таблицы `documents`, но поле `status:'ok'` то же — реального расхождения не найдено. Также в десктопе есть третья копия той же логики в `src/modules/readiness-calc.js` (для клиентского JS в Electron-рендерере) — не сверялась построчно с `mcp-server/readiness-calc.js`, но заявлена как единый источник ("Извлечены 1:1... чтобы Центр готовности и дашборд аутсорсера показывали ОДНО И ТО ЖЕ число") — стоит перепроверить построчно при следующей задаче на readiness, т.к. именно с тремя разными формулами уже была история рассинхрона в июне. |
| **Названия документов ВУ-07/ВУ-08 в реестре разделов** | `sections.js:196-197`: `'ВУ-07 Уведомление в военкомат о приёме военнообязанного.docx'`, `'ВУ-08 Уведомление в военкомат об увольнении военнообязанного.docx'` | `sections.js:196-197`: `'ВУ-07 Сведения о принятых и уволенных военнообязанных.docx'`, `'ВУ-08 Листок сообщения об изменениях сведений военнообязанного.docx'` | Названия файлов документов ВУ-07/08 в маппинге на раздел `VU_NOTICES` не совпадают между базами — либо сам генератор (`gen_vu.js`, который идентичен в обеих базах побайтово) выдаёт файл с другим именем, чем ожидает `sections.js`, в одной из версий, либо это просто рассинхрон переименования формы документа. Нужно свериться с `gen_vu.js`, какое имя файла реально используется, — не исследовано вглубь в рамках read-only обзора. |
| **Мониторинг НПА** | Встроен прямо в `main.js` (`checkNpaUpdates`, `NPA_WATCHLIST`, прямые запросы к `publication.pravo.gov.ru/api/Documents`) | Вынесен в отдельные модули `backend/npa-monitor.js` + `backend/npa-watchlist.js`, с фильтрацией через DeepSeek | Не расхождение в цифрах/формулах, но архитектурное расхождение той же функции — при переносе правок НПА-списка нужно не забывать оба места (`main.js` NPA_WATCHLIST в десктопе и `backend/npa-watchlist.js` в вебе), это два независимых списка актов, которые могут разойтись по составу. |
| **Инструмент проверки актуальности НПА-цитат** | Есть (`npa-audit.js`, раздел 1) | Не найден отдельный аналог | См. раздел 1 — по сути это "есть только в десктопе", но упомянуто и здесь, т.к. концептуально пересекается с `npa-monitor.js` веба (обе функции работают с одним и тем же `NPA_WATCHLIST`-подобным списком, но решают разные задачи — не спутать при развитии функциональности). |

## Сводка

- Раздел 1 (только в desktop): 8 пунктов.
- Раздел 2 (только в web): 6 пунктов.
- Раздел 3 (в обеих, но по-разному): 7 пунктов, включая подтверждённый неисправленный баг нормализации города в десктопе (2 места: `gen_p1.js`, `doc-generation.js`) и подтверждённый идентичный баг с плейсхолдером ЧОП в обеих базах.
